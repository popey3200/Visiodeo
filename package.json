{
  "name": "visiodeo-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Visiodeo - wrapper mobile (Capacitor)",
  "scripts": {
    "build:web": "echo \"Place your web files into the ./www folder (copy visiodeo.html → www/index.html)\"",
    "prepare:web": "cp -R visiodeo www || true",
    "cap:init": "npx cap init VisioDeo com.popey3200.visiodeo --web-dir=www",
    "cap:add:android": "npx cap add android",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.4"
  }
}
